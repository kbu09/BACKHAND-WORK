
#  How will you compare two lists?
# Ans = Direct Equality Check (==)
# This method checks if both lists have the same elements in the exact same order.
# If they are equal, it returns True; otherwise, it returns False.


l1 = [1, 2, 3]
l2 = [1, 2, 3]
print(l1 == l2)

  # op: True
